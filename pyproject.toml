[project]
name = "campaign-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kimlong42", email = "42.kimlong@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
campaign-lab = "campaign_lab:main"

[build-system]
requires = ["uv_build>=0.11.26,<0.12.0"]
build-backend = "uv_build"
